{
  "name": "rails-template",
  "private": true,
  "scripts": {
    "lint:format": "prettier --check '**/*'",
    "lint:format:fix": "prettier --write '**/*'"
  },
  "dependencies": {},
  "devDependencies": {
    "prettier": "^2.0.5"
  }
}
